import React from 'react';
import styles from './buttons.module.css'

const Buttons = ({btName}) => {
 
    return (
        <button  className={styles.NavbarButtons}>
        Login
      </button>
  )
}

export default Buttons;